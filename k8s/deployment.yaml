apiVersion: apps/v1
kind: Deployment
metadata:
  name: bytevault-deployment
  labels:
    app: bytevault
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bytevault
  template:
    metadata:
      labels:
        app: bytevault
    spec:
      containers:
      - name: bytevault
        image: fafiorim/bytevault:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
          name: http
        - containerPort: 3443
          name: https
        env:
        - name: ADMIN_USERNAME
          valueFrom:
            configMapKeyRef:
              name: bytevault-config
              key: admin-username
        - name: USER_USERNAME
          valueFrom:
            configMapKeyRef:
              name: bytevault-config
              key: user-username
        - name: ADMIN_PASSWORD
          valueFrom:
            secretKeyRef:
              name: bytevault-secrets
              key: admin-password
        - name: USER_PASSWORD
          valueFrom:
            secretKeyRef:
              name: bytevault-secrets
              key: user-password
        - name: FSS_API_KEY
          valueFrom:
            secretKeyRef:
              name: bytevault-secrets
              key: fss-api-key
        volumeMounts:
        - name: uploads-volume
          mountPath: /app/uploads
      volumes:
      - name: uploads-volume
        emptyDir: {}
